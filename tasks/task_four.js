// Declare a variable with an initial value
let count = 0;

// Specify the target value
let target = 10;

while (count < target) {

  console.log(count);

  // Increment the variable
  count++;
}




